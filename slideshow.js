function setup(){
    translate(window.innerWidth/2,window.innerHeight/2);
    canvas=createCanvas(1000,150);
    canvas.parent('slide');
    background(0);
}

function draw(){
    
}

function change(){
    
}